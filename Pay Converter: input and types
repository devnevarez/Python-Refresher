# Along with user input we also have to keep track of character types
# Lets create a script that determines pay per hour

hrs = input("Enter Hours:")
rate = input("Enter rate:")

# After creating variables we can call, we can now create a formula within its own variable
# Keeping in mind character types must align to avoid syntax errors

pay = float(hrs) * float(rate)

# We can now call the formula and display the user input

print("Pay: " + str(pay))

# Lets continue
# Now we can add conditionals, useful for calculating overtime pay

hrs = input("Enter Hours:")
h = float(hrs)
rate = input("Enter rate:")
r = float(rate)

# With if statements we can create code blocks that run if different conditions are met

if h <= 40:
    print("Pay: " h * r)
else:
    reg_time = h * r
    ot = (h - 40) * (r * .5)
    print("Pay: " reg_time + ot)

# Revisiting what I have so far and after completing some more lessons, lets create a function

def computepay(h, r):
    if h > 40 :
        return extra_time
    elif h <= 40 :
        return reg_time
    else :
        quit()

# I think this is a little bit neater as I consider regular time hours and overtime hours otherwise we stop the program
# all that is left is to define the variables

hrs = input("Enter Hours:")
h = float(hrs)
rate = input("Enter Hourly Rate:")
r = float(rate)

reg_time = h * r
ot = (h - 40) * (r * .5)
extra_time = reg_time + ot

p = computepay(h, r)
print("Pay", p)
